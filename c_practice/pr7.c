#include <stdio.h>

int main(void) {
    int arr[9] = {9, 8, 7, 6, 5, 4, 3, 2, 1};
    int counter = 0;
    count();
}

int count(int* arr, int x, int len) {
    if (len == 1) {
        return 
    }
    (*counter)+=count(arr, x, len);
}